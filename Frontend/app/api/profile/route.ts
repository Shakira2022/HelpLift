import { NextRequest, NextResponse } from "next/server"
import { BACKEND_TOKEN_COOKIE, BACKEND_URL } from "@/lib/backend"
async function call(req:NextRequest,method:string){const token=req.cookies.get(BACKEND_TOKEN_COOKIE)?.value;if(!token)return NextResponse.json({success:false,message:"Authentication required"},{status:401});try{const r=await fetch(`${BACKEND_URL}/api/users/me`,{method,headers:{"Content-Type":"application/json",Authorization:`Bearer ${token}`},body:method==="PUT"?await req.text():undefined,cache:"no-store"});return NextResponse.json(await r.json(),{status:r.status})}catch{return NextResponse.json({success:false,message:"Profile service is unavailable"},{status:503})}}
export const GET=(req:NextRequest)=>call(req,"GET"); export const PUT=(req:NextRequest)=>call(req,"PUT");
